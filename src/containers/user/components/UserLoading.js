import React from 'react';
// UIComponents
import { Loading, CenterContent } from '../../../components';

const UserLoading = () => (
  <CenterContent>
    <Loading color={'transparent'} />
  </CenterContent>
);

export default UserLoading;
